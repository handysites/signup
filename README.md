# HandySites rough draft

A static landing page designed for GitHub Pages.

## Preview locally

Open `index.html` directly, or run:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.

## Publish

Push these files to a GitHub repository and enable GitHub Pages from the repository settings. No build command is required.
